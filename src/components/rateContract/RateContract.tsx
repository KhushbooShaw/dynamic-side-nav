
import React from 'react';
import './RateContract.scss';

interface IRateContractProps {
};

const RateContract: React.FC<IRateContractProps> = (props: IRateContractProps) => {   
   
        return(
            <h1 className="page-header"> Rate Contract </h1>
        );    
}

export default RateContract;

