
import React from 'react';
import './ReleaseOrder.scss';

interface IReleaseOrderProps {
};

const ReleaseOrder: React.FC<IReleaseOrderProps> = (props: IReleaseOrderProps) => {   
   
        return(
            <h1 className="page-header"> Release Order </h1>
        );    
}

export default ReleaseOrder;

