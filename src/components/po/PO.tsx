
import React from 'react';
import './PO.scss';

interface IPOProps {
};

const PO: React.FC<IPOProps> = (props: IPOProps) => {   
   
        return(
            <h1 className="page-header"> PO </h1>
        );    
}

export default PO;

